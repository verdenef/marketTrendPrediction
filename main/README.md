# is108final - application

Application code lives here. POS (tasks, rules, docs) is in `../paws022/`.

Open the **parent workspace folder** in your executor IDE so both `paws022` and `main` are in the tree.