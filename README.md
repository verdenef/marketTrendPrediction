# is108final

**Sidecar layout:** POS in `paws022/`, application in `main/`.

Open **this folder** (workspace root) in your executor IDE - not only `main`.

See [docs/PROJECT_LAYOUT.md](paws022/docs/PROJECT_LAYOUT.md).